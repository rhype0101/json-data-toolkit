{
  "name": "json-data-toolkit",
  "version": "1.0.0",
  "description": "Filtering and aggregating JSON data based on specific criteria",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "jest": "^24.9.0"
  }
}
